# cache22 — incus expects /var/log/incus/ + incusd.log to exist before
# the daemon will start. The arch incus package doesn't ship a
# tmpfiles.d entry that creates them.
d /var/log/incus 0750 root root -
f /var/log/incus/incusd.log 0640 root root -
