# bash-completion for cache22-update
_cache22_update() {
    local cur prev words cword
    _init_completion || return

    local opts="--check --reboot --app-updates --if-idle --help"
    COMPREPLY=( $(compgen -W "$opts" -- "$cur") )
}
complete -F _cache22_update cache22-update
