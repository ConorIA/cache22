# cache22 — arch-server additions on top of arch-common.txt.
#
# Headless. Same content as cachy-server.txt — cockpit's package names
# are identical between cachyos repos and stock Arch.

# ─── Web admin (optional, but standard for headless boxes) ─
cockpit
cockpit-podman
cockpit-machines     # libvirt/qemu management
cockpit-storaged     # disks/lvm/btrfs
